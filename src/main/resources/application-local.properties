# Local development overrides (activate with --spring.profiles.active=local)

# Disable GCP metrics export locally (metrics still available via /actuator/metrics)
management.stackdriver.metrics.export.enabled=false
